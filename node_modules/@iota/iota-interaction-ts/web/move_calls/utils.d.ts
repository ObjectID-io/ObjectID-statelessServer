import { Transaction, TransactionArgument } from "@iota/iota-sdk/transactions";
export declare function getClockRef(tx: Transaction): TransactionArgument;
export declare function getControllerDelegation(tx: Transaction, controllerCap: TransactionArgument, packageId: string): [TransactionArgument, TransactionArgument];
export declare function putBackDelegationToken(tx: Transaction, controllerCap: TransactionArgument, delegationToken: TransactionArgument, borrow: TransactionArgument, packageId: string): void;
/**
 * Inserts placeholders related to sender and payment into transaction.
 *
 * This is required if wanting to call `tx.build`, as this will check if these values have been set.
 *
 * @param tx transaction to update
 */
export declare function insertPlaceholders(tx: Transaction): void;
