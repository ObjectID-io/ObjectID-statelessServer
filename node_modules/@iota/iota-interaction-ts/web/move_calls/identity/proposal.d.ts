import { SharedObjectRef } from "@iota/iota-sdk/dist/cjs/bcs/types";
import { ObjectRef } from "@iota/iota-sdk/transactions";
export declare function approve(identity: SharedObjectRef, capability: ObjectRef, proposalId: string, proposalType: string, packageId: string): Promise<Uint8Array>;
