import { SharedObjectRef } from "@iota/iota-sdk/dist/cjs/bcs/types";
import { ObjectRef } from "@iota/iota-sdk/transactions";
export declare function proposeSend(identity: SharedObjectRef, capability: ObjectRef, transferMap: [string, string][], packageId: string, expiration?: number): Promise<Uint8Array>;
export declare function executeSend(identity: SharedObjectRef, capability: ObjectRef, proposalId: string, objects: [ObjectRef, string][], packageId: string): Promise<Uint8Array>;
