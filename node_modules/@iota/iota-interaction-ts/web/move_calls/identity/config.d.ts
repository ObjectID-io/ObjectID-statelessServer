import { SharedObjectRef } from "@iota/iota-sdk/dist/cjs/bcs/types";
import { ObjectRef } from "@iota/iota-sdk/transactions";
export declare function proposeConfigChange(identity: SharedObjectRef, controllerCap: ObjectRef, controllersToAdd: [string, number][], controllersToRemove: string[], controllersToUpdate: [string, number][], packageId: string, expiration?: number, threshold?: number): Promise<Uint8Array>;
export declare function executeConfigChange(identity: SharedObjectRef, capability: ObjectRef, proposalId: string, packageId: string): Promise<Uint8Array>;
