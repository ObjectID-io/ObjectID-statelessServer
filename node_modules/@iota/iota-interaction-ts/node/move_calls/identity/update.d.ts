import { SharedObjectRef } from "@iota/iota-sdk/dist/cjs/bcs/types";
import { ObjectRef } from "@iota/iota-sdk/transactions";
export declare function proposeUpdate(identity: SharedObjectRef, capability: ObjectRef, didDoc: Uint8Array | undefined, packageId: string, expiration?: number): Promise<Uint8Array>;
export declare function executeUpdate(identity: SharedObjectRef, capability: ObjectRef, proposalId: string, packageId: string): Promise<Uint8Array>;
