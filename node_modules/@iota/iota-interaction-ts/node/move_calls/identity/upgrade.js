"use strict";
// Copyright 2020-2024 IOTA Stiftung
// SPDX-License-Identifier: Apache-2.0
Object.defineProperty(exports, "__esModule", { value: true });
exports.proposeUpgrade = proposeUpgrade;
exports.executeUpgrade = executeUpgrade;
const transactions_1 = require("@iota/iota-sdk/transactions");
function proposeUpgrade(identity, capability, packageId, expiration) {
    const tx = new transactions_1.Transaction();
    const cap = tx.objectRef(capability);
    const identityArg = tx.sharedObjectRef(identity);
    const exp = tx.pure.option("u64", expiration);
    tx.moveCall({
        target: `${packageId}::identity::propose_upgrade`,
        arguments: [identityArg, cap, exp],
    });
    return tx.build();
}
function executeUpgrade(identity, capability, proposalId, packageId) {
    const tx = new transactions_1.Transaction();
    const cap = tx.objectRef(capability);
    const proposal = tx.pure.id(proposalId);
    const identityArg = tx.sharedObjectRef(identity);
    tx.moveCall({
        target: `${packageId}::identity::execute_upgrade`,
        arguments: [identityArg, cap, proposal],
    });
    return tx.build();
}
//# sourceMappingURL=upgrade.js.map