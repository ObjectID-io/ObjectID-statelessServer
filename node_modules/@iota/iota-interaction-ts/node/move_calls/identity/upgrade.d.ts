import { SharedObjectRef } from "@iota/iota-sdk/dist/cjs/bcs/types";
import { ObjectRef } from "@iota/iota-sdk/transactions";
export declare function proposeUpgrade(identity: SharedObjectRef, capability: ObjectRef, packageId: string, expiration?: number): Promise<Uint8Array>;
export declare function executeUpgrade(identity: SharedObjectRef, capability: ObjectRef, proposalId: string, packageId: string): Promise<Uint8Array>;
