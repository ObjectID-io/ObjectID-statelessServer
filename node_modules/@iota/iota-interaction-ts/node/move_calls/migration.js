"use strict";
// Copyright 2020-2024 IOTA Stiftung
// SPDX-License-Identifier: Apache-2.0
Object.defineProperty(exports, "__esModule", { value: true });
exports.migrateDidOutput = migrateDidOutput;
const transactions_1 = require("@iota/iota-sdk/transactions");
const utils_1 = require("./utils");
function migrateDidOutput(didOutput, migrationRegistry, packageId, creationTimestamp) {
    const tx = new transactions_1.Transaction();
    const did_output = tx.objectRef(didOutput);
    const migration_registry = tx.sharedObjectRef(migrationRegistry);
    const clock = (0, utils_1.getClockRef)(tx);
    let timestamp;
    if (creationTimestamp) {
        timestamp = tx.pure.u64(creationTimestamp);
    }
    else {
        timestamp = tx.moveCall({
            target: "0x2::clock::timestamp_ms",
            arguments: [clock],
        });
    }
    tx.moveCall({
        target: `${packageId}::migration::migrate_alias_output`,
        arguments: [did_output, migration_registry, timestamp, clock],
    });
    return tx.build();
}
//# sourceMappingURL=migration.js.map